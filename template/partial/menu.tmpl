{{if eq .AuthLevel "auth"}}
    <form class="form-inline mt-2 mt-md-0" action="{{.BaseURI}}logout" method="get">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">你好, {{.username}} 注销</button>
     </form>
{{else}}
{{end}}
